{
  "name": "apps-worker",
  "private": true,
  "scripts": {
    "dev": "tsx watch --env-file=../../.env src/index.ts",

    "worker": "tsx src/index.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@leadforge/db": "*",
    "bullmq": "^5.61.0",
    "ioredis": "^5.8.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.20.6",
    "typescript": "^5"
  }
}
